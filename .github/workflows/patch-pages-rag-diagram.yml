name: Patch Pages RAG diagram

on:
  push:
    paths:
      - ".github/workflows/patch-pages-rag-diagram.yml"

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - run: echo "workflow parser ok"
